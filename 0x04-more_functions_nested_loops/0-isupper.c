#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @c: The number to be checked
 * Return: 1 for upper leteer or 0 for and else
 */

int _isupper(int c)

{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
